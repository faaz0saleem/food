# Developer instructions

- Use Node.js 18+ and npm scripts defined in `package.json`.
- For local setup: `npm install`, configure `.env`, then run `npm run seed` and `npm start`.
- Run tests with `npm test` before committing.
- Do not commit real secrets; keep credentials in environment variables.
- For production, deploy behind HTTPS and configure managed certificates.
